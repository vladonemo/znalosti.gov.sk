Build the project using Maven.

Run: `mvn clean install`

For production build (includes Docker image): `mvn clean install -Pproduction`

Check for compilation errors and report any failures with the relevant error messages.