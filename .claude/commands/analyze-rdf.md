Analyze RDF source data files in the project.

List and describe all RDF data sources in `src/main/resources/META-INF/resources/sourcedata/`.

For each file, report:
- File name and format (RDF/XML, Turtle, OWL, etc.)
- Size
- Brief description of contents (ontology name, vocabulary, dataset)

Also check `src/main/resources/META-INF/resources/db/` and `src/main/resources/META-INF/resources/db-refid-test-data/` for additional data files.
